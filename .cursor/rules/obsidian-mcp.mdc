---
description: Petralian vault paths — native I/O at desk; MCP for remote services only
alwaysApply: true
---

# Obsidian — Petralian

> **Tool choice:** `.cursor/rules/obsidian-vault-io.mdc` — **native only** at desk; no vault MCP locally.

**Project vault:** `D:\Obsidian\Obsidian\40_VSCode\Petralian`  
**Brain:** `D:\Obsidian\Obsidian\00_Brain`

| Context | Vault I/O | MCP |
|---------|-----------|-----|
| Desk (local Cursor) | Native `Read`/`Grep`/`Write`/`StrReplace` on `D:\Obsidian\...` | **No** `petralian-obsidian`, `obsidian-brain`, or `D:\` filesystem MCP |
| Cloud / iPhone | Native `Read`/`Grep` on `vault-petralian` git clone | URL servers only (e.g. OpenSEO); optional filesystem MCP on **clone path** in Dashboard |

Do not use `obsidian-brain` or `petralian-obsidian` MCP on the local machine — they are retired from `.cursor/mcp.json` and `.vscode/mcp.json`.
